<title>一次真实经验复现：EvoMap 如何让 Agent 在新线程里少绕弯</title>

<callout emoji="✅" background-color="light-green" border-color="green">
  <p><b>摘要：</b>本文用一个独立开发者的真实场景，复现 EvoMap / Evolver 如何把跨线程经验沉淀为 Gene / Capsule，并让 Cursor、Codex、Claude Code 这类 coding agent 在新线程里先 recall，再给方案。</p>
</callout>

<p><a type="url-preview" href="https://github.com/owenshen0907/evomap-agent-memory-demo">开源 Demo 仓库：evomap-agent-memory-demo</a></p>
<p><a type="url-preview" href="https://github.com/owenshen0907/evomap-agent-skill">evomap-agent-skill：安装与使用参考</a></p>

<h1>问题不是 Agent 不聪明，而是每个新线程都像第一次见你</h1>

<p>很多人开始使用 coding agent 后，很快会遇到一个反复出现的问题：同一个项目、同一套背景、同一批已经解释过的约束，只要换一个新线程，Agent 就可能重新追问一遍。</p>

<p>这不是模型能力差。多数时候，Agent 的反问是合理的。它不知道另一个线程里已经确认过数据库结构，不知道前一次任务里已经排除过某个方案，也不知道某个脚本为什么不能直接复用。于是用户不得不重复解释，Agent 也会在已经走过的路上继续试错。</p>

<p>EvoMap / Evolver 要解决的不是“让 Agent 永远不提问”，而是让 Agent 少问那些已经被验证过的问题。它把任务过程里的有效经验沉淀下来，让新线程先 recall 相关 Gene / Capsule，再进入方案设计。</p>

<p><b>【图 1：多线程经验如何被 EvoMap 复用】</b></p>

<p>这篇文章不从协议开始，而从一个可复现的案例开始：如果一个 Agent 已经在历史线程里学会了“网站词库和视频产物如何互相反哺”，新线程是否还能直接使用这条经验。</p>

<h1>Skill 解决固定流程，Evolver 解决经验变化</h1>

<p>Skill 很适合固定流程。比如告诉 Agent：生成视频前先检查素材目录，改生产数据前必须确认，发布前要跑哪些命令。只要任务边界稳定，Skill 是非常有效的操作说明。</p>

<p>但长期项目里的经验并不总是稳定流程。很多经验来自变化：用户纠正了一次错误判断，某个旧约束不再适用，一个原本只服务网站的素材开始被视频流程复用。把所有变化都写回 Skill，Skill 会越来越长，也可能把旧条件带进新任务。</p>

<p>Evolver 处理的是另一类问题。它从任务信号中识别可复用经验，提炼成 Gene；当这条经验经过验证，或者形成了有价值的正向经验和错误示例，再包装成 Capsule。新线程 recall 到这些资产后，Agent 就能先站在历史经验上思考，而不是从空白开始。</p>

<p><b>【图 2：Skill 和 Evolver 的边界】</b></p>

<p>一个简单的判断方式是：</p>

<table>
  <thead>
    <tr><th background-color="light-gray">类型</th><th background-color="light-gray">更适合放在哪里</th><th background-color="light-gray">例子</th></tr>
  </thead>
  <tbody>
    <tr><td>稳定流程</td><td>Skill</td><td>每次发版前必须跑测试</td></tr>
    <tr><td>已验证经验</td><td>Gene / Capsule</td><td>某个项目里网站词库是视频流程的事实源</td></tr>
    <tr><td>敏感动作</td><td>用户确认</td><td>上传素材、改库、发布、消费 credits</td></tr>
  </tbody>
</table>

<h1>普通用户怎么接入：先让 Agent 帮你装</h1>

<p>普通用户不需要先理解 GEP、A2A、GDI 这些概念。官网的推荐路径里，大多数用户可以先从 CLI 开始，因为 CLI 会处理注册、令牌存储和协议升级；对 Cursor、Claude Code、终端类 Agent 都更直接。</p>

<p>你可以直接把下面这段话发给当前 Agent：</p>

<pre lang="text" caption="给 Agent 的安装提示"><code>请安装 @evomap/evolver，并在当前项目里配置对应平台的 hooks。
如果是 Cursor，用 evolver setup-hooks --platform=cursor。
如果是 Codex，用 evolver setup-hooks --platform=codex。
如果是 Claude Code，用 evolver setup-hooks --platform=claude-code。
完成后告诉我是否需要重启，以及 hooks 是否注册成功。</code></pre>

<p>如果希望 Agent 更稳地遵守 EvoMap 的使用边界，可以再安装 <code>evomap-agent-skill</code>：</p>

<pre lang="bash" caption="安装命令"><code>npm install -g @evomap/evolver
evolver setup-hooks --platform=codex
npx skills add owenshen0907/evomap-agent-skill -g -y</code></pre>

<p>第一次试用建议保持三个默认原则：本地、可审查、零自动消费。不要默认开启自动购买、自动发布、自动消耗 credits 的功能。Evolver 应该先帮助 Agent 看见经验，而不是替用户跳过确认。</p>

<p><b>【图 3：三步接入 EvoMap】</b></p>

<p>接入后，Agent 的关键节点会把可审查的任务信号交给 Evolver。新线程开始时，Agent 可以 recall 相关 Gene / Capsule，并把它们作为上下文使用。这里改变的是 Agent 看到的经验背景，不是让 Agent 自动执行高风险动作。</p>

<h1>一个真实但足够小的案例：日语工作坊和跟读视频</h1>

<p>原始场景来自一个独立开发者的日语学习产品。实际系统里有网站、素材、数据库、对象存储和视频流程。为了让它适合官网演示和开源复现，本文把它收敛成两个工作面。</p>

<table>
  <thead>
    <tr><th background-color="light-gray">工作面</th><th background-color="light-gray">已有内容</th><th background-color="light-gray">新产生的价值</th></tr>
  </thead>
  <tbody>
    <tr><td>日语工作坊网站</td><td>17000 多个单词、17000 多张照片、读音和解释</td><td>为视频提供稳定 word_id、图片和音频</td></tr>
    <tr><td>日语跟读视频</td><td>按场景筛词，生成跟读合集</td><td>产生场景标签、例句图片、旁白音频</td></tr>
  </tbody>
</table>

<p>这两个工作面不是单向关系。网站给视频提供词库和素材；视频生成过程中产生的场景、例句图片、旁白音频，又应该回到网站展示。</p>

<p><b>【图 4：日语工作坊与跟读视频反馈循环】</b></p>

<blockquote>日语工作坊网站的 <code>word_id</code> 是事实源；视频读取 <code>scene_word_set</code>；视频生成的场景、例句图片和音频写入 <code>word_learning_assets</code>，再反哺回网站展示。</blockquote>

<p>在 Evolver 里，它可以对应两个资产：</p>

<table>
  <thead>
    <tr><th background-color="light-gray">类型</th><th background-color="light-gray">名称</th><th background-color="light-gray">作用</th></tr>
  </thead>
  <tbody>
    <tr><td>Gene</td><td>workshop-shadowing-feedback-loop</td><td>描述网站和视频之间的稳定反馈关系</td></tr>
    <tr><td>Capsule</td><td>workshop-shadowing-positive-feedback-case</td><td>保存一次验证过的正向经验，说明如何最小化复现</td></tr>
  </tbody>
</table>

<h1>同一个需求，Agent 的起手式应该改变</h1>

<p>新线程里只给 Agent 这段需求：</p>

<pre lang="text" caption="新线程里的需求"><code>日语工作坊网站已经有 17000 多个日语单词和对应照片。
我想按场景筛选一批词，生成跟读视频合集；
视频生成过程中会产生单词场景、例句图片和音频，
这些又要反哺回网站展示。
你帮我设计一个最小复现流程。</code></pre>

<p>没有历史经验时，Agent 先问表结构、素材位置、场景分类、视频脚本入口、回写方式，都是合理的。但如果这些信息已经在多个历史线程里确认过，新线程仍然从这些基础问题开始，说明经验没有被继承。</p>

<p>接入 EvoMap 后，理想的变化不是“Agent 不问问题”，而是“Agent 先复用经验，再只问真正需要确认的风险点”。它应该先 recall 到前面的 Gene / Capsule，然后直接给出更贴近项目现状的最小方案：</p>

<ol>
  <li>以日语工作坊 <code>word_id</code> 为事实源，不复制一套视频专用单词表。</li>
  <li>先选一个最小场景，例如“餐厅”。</li>
  <li>生成 <code>scene_word_set_restaurant.json</code>。</li>
  <li>为少量单词生成例句、例句图片和跟读音频。</li>
  <li>生成一个 30 秒视频合集 manifest。</li>
  <li>把场景标签、例句图片 <code>object_key</code>、音频 <code>object_key</code> 写入 <code>word_learning_assets</code>，供网站展示。</li>
</ol>

<p><b>【图 5：接入 EvoMap 后，Agent 先复用经验再给方案】</b></p>

<p>它仍然应该向用户确认高风险动作，例如是否允许新增或修改网站素材 manifest，是否允许真实视频渲染，是否允许上传素材，是否允许发布网站。少绕弯不等于少确认。</p>

<h1>开源 demo 如何复现这个经验</h1>

<p>这个 demo 不执行真实上传、不改生产数据库，也不渲染真实视频。它验证的是更前置、更关键的能力：在新线程混入干扰信息时，Agent 能否 recall 到正确经验，并给出正确方向。</p>

<pre lang="bash" caption="本地验证命令"><code>git clone https://github.com/owenshen0907/evomap-agent-memory-demo.git
cd evomap-agent-memory-demo
python3 demo.py
python3 scripts/validate_recall.py
python3 -m unittest discover</code></pre>

<p>验收时看这些输出：</p>

<table>
  <thead>
    <tr><th background-color="light-gray">验收项</th><th background-color="light-gray">预期</th></tr>
  </thead>
  <tbody>
    <tr><td>历史线程存在</td><td>输出 SIMULATED_SOURCE_THREADS</td></tr>
    <tr><td>干扰线程存在</td><td>输出 DISTRACTOR_THREADS</td></tr>
    <tr><td>新线程 recall</td><td>输出 RECALL_HITS</td></tr>
    <tr><td>命中 Gene</td><td>workshop-shadowing-feedback-loop</td></tr>
    <tr><td>命中 Capsule</td><td>workshop-shadowing-positive-feedback-case</td></tr>
    <tr><td>方案命中关键结构</td><td>scene_word_set、video_collection_manifest、word_learning_assets</td></tr>
    <tr><td>自动化结果</td><td>RESULT PASS</td></tr>
    <tr><td>单测结果</td><td>5 tests OK</td></tr>
  </tbody>
</table>

<p><b>【图 6：demo 验收输出拆解】</b></p>

<p>这个 demo 的意义不是证明 EvoMap 已经完成生产任务，而是证明新线程不需要重新猜测“网站和视频是什么关系”。它可以先用历史经验定位方向，再把精力放在真正需要执行确认的地方。</p>

<h1>怎么判断效果不是心理作用</h1>

<p>评估 EvoMap 的效果，不应该只看 Agent 的语气是不是更自信，而应该看行为有没有变化。</p>

<table>
  <thead>
    <tr><th background-color="light-gray">指标</th><th background-color="light-gray">没有 EvoMap 时</th><th background-color="light-gray">接入 EvoMap 后</th></tr>
  </thead>
  <tbody>
    <tr><td>上下文重复</td><td>新线程重新问网站和视频关系</td><td>先 recall 已沉淀的反馈循环</td></tr>
    <tr><td>反问质量</td><td>问大量基础背景</td><td>只问真实执行前必须确认的风险点</td></tr>
    <tr><td>方案具体度</td><td>停留在“先确认结构”</td><td>直接提出 scene_word_set、video_collection_manifest、word_learning_assets</td></tr>
    <tr><td>抗干扰能力</td><td>可能被首页配色、片头音乐、字幕模板带偏</td><td>仍命中日语工作坊和跟读视频的 Gene / Capsule</td></tr>
    <tr><td>可验证性</td><td>只能凭感觉说“好像更懂我”</td><td>有 validate_recall.py 和单测输出作为证据</td></tr>
  </tbody>
</table>

<p><b>【图 7：进化效果指标】</b></p>

<p>真正有价值的变化，是 Agent 的问题变少了，但不是因为它忽略风险，而是因为它把提问集中到用户真正需要决策的位置。</p>

<h1>安全边界：recall 不等于自动执行</h1>

<p>让 Agent 先 recall，不等于允许它自动执行生产动作。经验资产可以帮助 Agent 更快进入正确方案，但不能替用户跳过风险确认。</p>

<p>在这个 case 里，可以沉淀：日语工作坊和跟读视频之间的关系；<code>word_id</code> 是事实源；视频读取 <code>scene_word_set</code>；视频产物写回 <code>word_learning_assets</code>；demo 验收命令和 <code>PASS</code> 结果。</p>

<p>不应该沉淀：真实数据库密码；真实 OSS / 存储桶密钥；生产服务器 IP 和 token；长期有效的 signed URL；未经用户确认的自动上传、自动发布、自动购买配置。</p>

<p><b>【图 8：Evolver 安全边界】</b></p>

<p>更直接地说：Agent 可以先给方案，但写 migration、真实视频渲染、上传素材、发布网站，都应该继续向用户确认。</p>

<h1>结论：先从一个会重复出现的小场景开始</h1>

<p>EvoMap 的价值不是让 Agent 自动完成所有工程动作，而是解决一个更常见的问题：Agent 不必每个新线程都从零理解你。</p>

<p>Skill 负责固定流程，Evolver 负责把真实使用中的有效经验留下来。对普通用户来说，最直接的价值是少解释、少反问、少绕弯。对团队演示来说，这个 demo 也足够小：只要能复现 recall 命中 Gene / Capsule，并让新线程给出正确结构，就能说明 Evolver 已经开始对 Agent 起作用。</p>

<p>如果想试用，不需要一开始就让 EvoMap 记住所有东西。先选一个会重复出现、但每次都需要重新解释的小场景。把它跑通，再让 Agent 在下一个线程里验证是否能 recall 到正确经验。</p>

<h1>参考资料</h1>

<ul>
  <li><a href="https://evomap.ai/zh/learn/connect-ai-agent">EvoMap 官方指南：如何连接您的 AI 智能体</a></li>
  <li><a href="https://github.com/owenshen0907/evomap-agent-skill">evomap-agent-skill</a></li>
  <li><a href="https://github.com/owenshen0907/evomap-agent-memory-demo">本文开源 demo</a></li>
</ul>
